import './globals.css'
export const metadata = { title: 'Proposal Generator', description: 'AI-powered freelance proposals' }
export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>
}
